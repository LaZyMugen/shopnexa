// testSupabase was a temporary debug route; removed to keep repository clean.
// If you need a test route later, re-add a dedicated dev-only router and mount it
// conditionally when NODE_ENV !== 'production'.

// Intentionally left blank to avoid accidental usage.

export default null;
